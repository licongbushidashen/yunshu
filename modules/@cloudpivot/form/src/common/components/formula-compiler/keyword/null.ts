import { Formula } from '../type';

const NULL:Formula.KeywordParam = {
  name: 'NULL',
  paramType: Formula.ParamType.VOID,
  sourceType: Formula.SourceType.EDIT,
  value: null,
};

export default NULL;
