import { Formula } from '../type';

const TRUE:Formula.KeywordParam = {
  name: 'TRUE',
  paramType: Formula.ParamType.BOOL,
  sourceType: Formula.SourceType.EDIT,
  value: true,
};

export default TRUE;
